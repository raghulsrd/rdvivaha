# .env, .sql, config files-ஐ browser-ல open பண்ண முடியாம பண்ணுது
<FilesMatch "\.(env|sql|md|log)$">
    Require all denied
</FilesMatch>

<FilesMatch "^(config|db|helpers)\.php$">
    Require all denied
</FilesMatch>

# Directory listing off
Options -Indexes
